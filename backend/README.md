```shell
python manage.py runserver 0.0.0.0:8000 # Entrar a localhost:8000

python manage.py makemigrations

python manage.py migrate

python manage.py flush # Borrar los registros pero no las tablas
```
